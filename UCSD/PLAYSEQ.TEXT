if humanSide = gameSide then
   begin
    humanFlag := TRUE;
    
    {save current game state}
    SaveMove;
    
    playerMove(playMove, lastMove, gameSide);
    if humanSide <> gameSide then
     i := 1;
    if pieceCount = -1 then
     exit(program);
    if i = 1 then
     goto 0;
    {$I cthreat.text}
   0:
   end
  else
   begin
    {clear killer move slots}
    sPage := BASE2;
    dataSize := 8;
    for j := 1 to gamePly do
     begin
      offset := 3000 + (j * 16);
      playMove.id := 99;
      DataOps(1, sPage, dataSize, offset, playMove);
      offset := offset + 8;
      DataOps(1, sPage, dataSize, offset, playMove);
     end;
     
    {opening move selection}
    if openFlag then
     begin
      moveScore := 0;
      sPage := BASE3;
      {select random opening if first white move}
      if (gameMove = 1) and (gameSide = 0) then
       begin
        offset := OPENLIB + ((Rnd_Int(28) - 1) * 48);
        dataSize := 2;
        DataOps(2, sPage, dataSize, offset, playMove.id);
        offset := offset + 2;
        DataOps(2, sPage, dataSize, offset, playMove.startSq);
        offset := offset + 2;
        DataOps(2, sPage, dataSize, offset, playMove.endSq); 
       end
      else
       {search for matching openings}
       begin
        sPage := BASE3;
        dataSize := 2;
        offset1 := OPENTEMP;
        offset2 := OPENLIB;
        sideOffset := 0;
        repeat
         j := 0;
         found := 0;
         repeat
          {read opening move}
          offset := offset2 + j;
          DataOps(2, sPage, dataSize, offset, tempMove.id);
          offset := offset + 2;
          DataOps(2, sPage, dataSize, offset, tempMove.startSq);
          offset := offset + 2;
          DataOps(2, sPage, dataSize, offset, tempMove.endSq);
          tempMove.link := NIL;
          {read actual move}
          offset := moveLoc + j;
          DataOps(2, sPage, dataSize, offset, moveStore.id);
          offset := offset + 2;
          DataOps(2, sPage, dataSize, offset, moveStore.startSq);
          offset := offset + 2;
          DataOps(2, sPage, dataSize, offset, moveStore.endSq);
          moveStore.link := NIL;
          {check if match}
          if tempMove = moveStore then
           begin
            j := j + 6;
            if j > openPoint then
             found := 1;
           end
          else
           begin
            offset2 := offset2 + 48;
            if offset2 > 1343 then
             j := openPoint + 1
            else
             j := 0;
           end;
         until j > openPoint;
         {check if matched complete move sequence}
         if found = 1 then
          begin
           sideOffset := Succ(sideOffset);
           {store pointer to found opening line}
           DataOps(1, sPage, dataSize, offset1, offset2);
           offset1 := offset1 + 2;
           offset2 := offset2 + 48;
          end;
        until offset2 > 1343;
        {randomly select one of the matching opening lines}
        if sideOffset > 0 then
         begin
          offset := OPENTEMP + ((Rnd_Int(sideOffset) - 1) * 2);
          DataOps(2, sPage, dataSize, offset, sideOffset);
          offset := sideOffset + openPoint + 6;
          DataOps(2, sPage, dataSize, offset, playMove.id);
          offset := offset + 2;
          DataOps(2, sPage, dataSize, offset, playMove.startSq);
          offset := offset + 2;
          DataOps(2, sPage, dataSize, offset, playMove.endSq);
          if (openPoint + 6) = 42 then
           openFlag := FALSE;
         end
        else
         begin
          openFlag := FALSE;
          gotoxy(20, 7);
          write('thinking...');
          sPage := BASE1;
          dataSize := 8;
          ProcessMove(moveScore, aVal, bVal, playMove, lastMove);
         end; 
       end;
     end
    else
     begin
      gotoxy(20, 7);
      write('thinking...');
      ProcessMove(moveScore, aVal, bVal, playMove, lastMove);
     end;
   end;
   
{save move}
if openFlag then
 begin
  sPage := BASE3;
  dataSize := 2;
  openPoint := openPoint + 6;
  offset := moveLoc + openPoint;
  DataOps(1, sPage, dataSize, offset, playMove.id);
  offset := offset + 2;
  DataOps(1, sPage, dataSize, offset, playMove.startSq);
  offset := offset + 2;
  DataOps(1, sPage, dataSize, offset, playMove.endSq);
  if openPoint = 42 then
   openFlag := FALSE;
 end;

{update move list}
sPage := BASE2;
dataSize := 8;
offset := PLAYLIST + gamePointer;
DataOps(1, sPage, dataSize, offset, playMove);
gamePointer := gamePointer + 8;
moveStore.id := 99;
offset := offset + 8;
DataOps(1, sPage, dataSize, offset, moveStore);








